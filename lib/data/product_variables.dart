import 'package:flutter/material.dart';

class ProductValues {
  static const textPadding = EdgeInsets.all(25);
  static const sliverAppBarShape = RoundedRectangleBorder(
    borderRadius: BorderRadius.only(
      bottomLeft: Radius.circular(20),
      bottomRight: Radius.circular(20),
    ),
  );
  static const listAppBarTitle = 'Zodiac Listesi';
}
